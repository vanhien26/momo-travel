'use client';

/**
 * Placeholder – trang tim-kiem giờ redirect sang /ve-may-bay/[routeSlug] hoặc hiển thị fallback.
 * File này giữ lại để tránh lỗi "Failed to read source code" từ tools tham chiếu file cũ.
 */
export function TimKiemClient() {
  return null;
}
